// @flow
import * as React from 'react';
import PropTypes from 'prop-types';
import { Consumer } from '../initializer';

type GateProps = {
    onlyLogin: boolean,
    role: string,
};

class Gate extends React.Component <GateProps> {

}